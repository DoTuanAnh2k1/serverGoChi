apiVersion: v1
kind: Service
metadata:
  name: mgt-frontend
  namespace: default
  labels:
    app: mgt-frontend
spec:
  selector:
    app: mgt-frontend
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
